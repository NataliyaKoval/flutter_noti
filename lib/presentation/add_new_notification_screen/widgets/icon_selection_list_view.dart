import 'package:flutter/material.dart';

class IconSelectionListView extends StatelessWidget {
  const IconSelectionListView({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      scrollDirection: Axis.horizontal,
      children: [],
    );
  }
}
